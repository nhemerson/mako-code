# Mako Configuration Template
# Copy to mako.conf and modify values

# ========================
# Core Application Settings
# ========================
HOST="127.0.0.1"  # Bind address (0.0.0.0 for external access)
PORT=8000          # API server port
ENVIRONMENT="development"  # [development|production]
LOG_LEVEL="info"   # [debug|info|warning|error]

# ==================
# Path Configuration
# ==================
DATA_DIR="./data"   # Persistent data storage
LOG_DIR="./logs"    # Application logs
CACHE_DIR="./cache" # Temporary files

# ======================
# Development Features
# ======================
DEV_RELOAD="true"    # Enable auto-reload (disable in production)
DEBUG_TOOLBAR="true" # Show debugging interface

# ======================
# Security
# ======================
API_KEY="${MAKO_API_KEY}"        # Set in environment
ADMIN_TOKEN="${MAKO_ADMIN_TOKEN}" # Set in environment

# ======================
# Package Management
# ======================
PYTHON_MIN_VERSION="3.11.0"  # Minimum Python version
PYTHON_VENV_PATH=".venv"     # Virtual environment location

# ======================
# Third-Party Integrations
# ======================
# GOOGLE_ANALYTICS_ID=""  # Uncomment to enable
# SENTRY_DSN=""           # Error monitoring service

# ======================
# Advanced Settings
# ======================
# UVICORN_WORKERS=4      # Production: CPU cores + 1
# MAX_UPLOAD_SIZE=100    # MB
# DATABASE_TIMEOUT=30    # Seconds 

# ======================
# Dependency Management
# ======================
UV_ENABLED="true"        # Enable uv package installer
UV_PATH="${HOME}/.cargo/bin/uv"  # Path to uv executable
FALLBACK_PIP="true"      # Use pip if uv is not available
PIP_EXTRA_INDEX=""       # Additional package index URL (optional)

# ======================
# Version Requirements
# ======================
NODE_MIN_VERSION="16.0.0"    # Minimum Node.js version
NODE_ENV="development"       # Node environment [development|production]

# ======================
# Package Management
# ======================
PACKAGE_CACHE_DIR=".package-cache"  # Package download cache
OFFLINE_MODE="false"               # Work with cached packages only
STRICT_VERSIONS="true"            # Enforce exact version matches 